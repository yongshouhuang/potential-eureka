# ADR-005 · PvP 网络模型

- **Context**：A4 PvP 竞技（长线目标），但 MVP 不做实时手动 PvP（A4⑥边界）。
- **Decision**：**MVP 不做实时 PvP；核心层用异步/AI 代理**对战，完全复用 B4 战斗规则 + 属性归一（防 R1/R5 碾压）。预留 `_matchmaking` 接口供未来实时扩展。
- **Consequences**：✓ 省实时网络/反作弊复杂度、复用战斗代码、lean 可达；✗ 非真正实时对抗（可接受）。实时扩展点明确定义，避免后期返工。
